const styles = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '60%',
    height: '54%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
  },
} 

export default styles;

export const stylesResult = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '40%',
    height: '86%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
  },
} 

export const stylesSignUp = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '60%',
    height: '78%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
  },
} 
