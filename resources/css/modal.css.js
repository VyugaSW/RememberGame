const styles = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '60%',
    height: '54%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
    padding: '0'
  },
} 

export default styles;

export const stylesResult = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '40%',
    height: '86%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
    padding: '0'
  },
} 

export const stylesSignUp = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '60%',
    height: '84%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
    padding: '0'
  },
} 

export const stylesSignIn = {
  overlay: {
    position: 'fixed',
    zIndex: 1020,
    top: 0,
    left: 0,
    width: '100vw',
    height: '100vh',
    background: 'var(--background-modal-overlay)',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  content: {
    background: 'var(--background-modal)',
    width: '60%',
    height: '60%',
    margin: 'auto',
    border: 'none',
    borderRadius: '0.3rem',
    padding: '0'
  },
} 
